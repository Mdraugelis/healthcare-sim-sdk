# @package _global_
# Regime E — Model Drift
# Model AUC drifts from 0.80 to 0.60 between weeks 20-40.
regime: model_drift
model:
  auc:
    base: 0.80
    change_points:
      - [20, 0.80]
      - [40, 0.60]
    interpolation: linear
